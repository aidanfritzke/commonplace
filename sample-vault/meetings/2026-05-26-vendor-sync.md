# Vendor Sync — Acme API — May 26

Present: me, Devon. Acme: Lena (account mgr), Tomás (eng).

- Acme is deprecating v2 of the API in October. We're on v2.
- A v3 migration guide exists, but the auth flow changed (OAuth → API keys + HMAC).
- Tomás will send a sandbox key for testing.

Risk: October is tight if we don't start by August.

## Action items
- [ ] Devon — spike the v3 HMAC auth against the sandbox
- [ ] Me — add the v3 migration to Q3 scope, flag it to Sam
- [ ] Lena — send updated pricing for the v3 tier
